name: flutter_torico_id_login
description: A new Flutter plugin.
version: 0.0.2
publish_to: none
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_inappwebview: ^5.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.maru.flutter_torico_id_login
        pluginClass: FlutterToricoIdLoginPlugin
      ios:
        pluginClass: FlutterToricoIdLoginPlugin
